@if (Request.IsAuthenticated)
{
    @Html.ActionLink("Skapa gloslista", "Create", "List")
    @Html.Action("Stored", "List")
}
else
{
    @Html.ActionLink("Logga in", "Logon", "Account")
    @:eller
    @Html.ActionLink("Skapa ett konto", "Invite", "Account")
}